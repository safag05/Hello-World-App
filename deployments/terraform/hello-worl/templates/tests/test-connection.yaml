apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "hello-worl.fullname" . }}-test-connection"
  labels:
{{ include "hello-worl.labels" . | indent 4 }}
  annotations:
    "helm.sh/hook": test-success
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args:  ['{{ include "hello-worl.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
